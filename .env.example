# open-data-jalisco — example configuration
# Copia este archivo a `.env` y ajusta valores. Nunca commitees `.env`.
#
# Para levantar TODO con Docker:  cp .env.example .env  &&  docker compose up -d --build
# Para dev local (sólo Postgres en Docker, API/CLI con uv): docker compose up -d postgres

# --- Despliegue (Docker compose) ---
# Estas variables sólo las usa docker-compose. En local con `uv run` se ignoran.
# Cámbialas para producción; con los defaults el stack arranca tal cual.
POSTGRES_USER=odj
POSTGRES_PASSWORD=odj            # CÁMBIALA en producción
POSTGRES_DB=open_data_jalisco
POSTGRES_PORT=5432
# Dónde escucha el sitio. http://localhost = HTTP en :80. Un dominio real
# (p.ej. odj.example.com) activa HTTPS automático con Let's Encrypt.
SITE_ADDRESS=http://localhost
HTTP_PORT=80
HTTPS_PORT=443
# Extras de la imagen de la API. Vacío = embedder `dummy` (ligero). Pon
# `local-embed` si usas EMBEDDING_PROVIDER=local_st o RERANK_PROVIDER=cross_encoder
# (instala sentence-transformers/torch; imagen mucho más grande y build más lento).
API_EXTRAS=
# Nota: dentro de compose el DATABASE_URL se arma solo con las vars de arriba
# (host `postgres`). El DATABASE_URL de abajo es sólo para dev local con `uv`.

# --- Entorno ---
ENVIRONMENT=local
LOG_LEVEL=INFO

# --- Base de datos (Postgres + pgvector) ---
# Para uso local con docker-compose:
DATABASE_URL=postgresql+psycopg://odj:odj@localhost:5432/open_data_jalisco

# --- Raw storage local ---
# Carpeta donde se guardan los documentos originales. Idempotente, organizada por hash.
RAW_STORAGE_PATH=./data/raw

# --- Manifests ---
# Carpeta donde se escriben los manifests JSON de integridad técnica.
MANIFESTS_DIR=./datasets/manifests

# --- Embeddings ---
# Providers disponibles:
#   - "dummy":    vector determinista por SHA-256. Sin red, sin API key, SIN semántica
#                 real. Útil para tests, inútil para búsqueda.
#   - "local_st": sentence-transformers ejecutado localmente (CPU o CUDA). Requiere
#                 instalar el extra: `uv sync --extra local-embed`. Modelo por defecto
#                 `intfloat/multilingual-e5-small` (384d, multilingüe, ~470 MB).
EMBEDDING_PROVIDER=dummy
EMBEDDING_MODEL=dummy-v1
# IMPORTANTE: si cambias la dimensión después de crear las tablas, debes recrear la
# columna pgvector. Para MVP usamos 384 (compatible con multilingual-e5-small).
EMBEDDING_DIMENSION=384
# Sólo aplica a `local_st`. Ignorado por `dummy`.
EMBEDDING_DEVICE=cpu

# Para activar el embedder local descomenta y reemplaza los tres valores anteriores:
# EMBEDDING_PROVIDER=local_st
# EMBEDDING_MODEL=intfloat/multilingual-e5-small
# EMBEDDING_DIMENSION=384

# --- Reranking (opcional, mejora la precisión de búsqueda) ---
# Etapa cross-encoder que reordena el top-N del vector antes de cortar al limit.
# Corrige el caso "presupuesto municipal", donde el embedder chico no separa los
# documentos del *Estado* de Jalisco de los del municipio. Reusa el extra
# `local-embed` (CrossEncoder viene con sentence-transformers, no instala nada nuevo).
#   - "none":          desactivado (default). Cero coste, comportamiento idéntico.
#   - "cross_encoder": rerankea con un cross-encoder multilingüe (~470 MB).
RERANK_PROVIDER=none
RERANK_MODEL=cross-encoder/mmarco-mMiniLMv2-L12-H384-v1
RERANK_DEVICE=cpu
# Candidatos recuperados (rerankeados / filtrados por jurisdicción) antes de
# cortar al limit. Más alto = mejor recall antes del rerank, más lento.
RERANK_POOL=50

# --- Scraping ---
SCRAPER_USER_AGENT=open-data-jalisco/0.1 (+https://github.com/your-org/open-data-jalisco)
SCRAPER_TIMEOUT_SECONDS=30
SCRAPER_MAX_RETRIES=3
SCRAPER_RETRY_BACKOFF_SECONDS=2

# --- Chunking ---
CHUNK_MAX_CHARS=1800
CHUNK_OVERLAP_CHARS=200
CHUNK_MIN_CHARS=120

# --- API ---
API_HOST=0.0.0.0
API_PORT=8000

# --- Agente de respuestas (opcional) ---
# Habilita POST /ask: el agente busca en los documentos (varias veces si hace
# falta), razona y responde con citas. Habla la API OpenAI Chat Completions, así
# que sirve CUALQUIER proveedor compatible — Gemini, OpenAI, Groq, un servidor
# local… el modelo lo eliges tú. Sin LLM_API_KEY el agente queda apagado
# (POST /ask -> 503) y el resto de la API no cambia. La key NUNCA va al repo.
#
# LLM_PROVIDER elige el endpoint por defecto: google | openai | groq |
# openrouter | custom. Para "custom" (servidor local u otro host) fija
# LLM_API_BASE; en los demás se deduce solo (y un LLM_API_BASE explícito gana).
LLM_PROVIDER=google
LLM_API_KEY=
LLM_MODEL=gemini-2.5-pro
# Router opcional: modelo barato que clasifica la intención (saludo / fuera de
# tema / búsqueda) antes del loop caro. Vacío = sin router (todo va a búsqueda).
LLM_ROUTER_MODEL=gemini-2.5-flash
LLM_MAX_ITERS=5
LLM_TIMEOUT_SECONDS=60
LLM_TEMPERATURE=0.2
# Otros proveedores (mismo patrón, sólo cambia provider + modelo + key):
#   OpenAI:     LLM_PROVIDER=openai      LLM_MODEL=gpt-4o          LLM_ROUTER_MODEL=gpt-4o-mini
#   Groq:       LLM_PROVIDER=groq        LLM_MODEL=llama-3.3-70b-versatile
#   OpenRouter: LLM_PROVIDER=openrouter  LLM_MODEL=...
#   Local:      LLM_PROVIDER=custom      LLM_API_BASE=http://localhost:11434/v1  LLM_MODEL=llama3.1
